#pragma once
#include <vector>

/// находит самый часто встречающийся элемент
int findOftenElement(std::vector<int>& sortArray);

/// меняет два элемента массива местами
void swap(std::vector<int>& sortArray, int index1, int index2);